import type { PestCandidate } from "./types";

export function matchPests(
  crop: string,
  environmentData: Record<string, unknown>,
): PestCandidate[] {
  // TODO: Implement in Phase 3
  return [];
}
